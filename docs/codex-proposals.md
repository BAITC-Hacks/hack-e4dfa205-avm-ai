# Предложения Codex: независимый запуск и ревью

23.09.2026, Астана (UTC+5). Проверенная ветка `main`, HEAD `ee6aa94bd39650803a5a631a317c5dfced728205`. Начальное рабочее дерево чистое. Статус: предложения для распределения задач, **патчи не применены**, реализация не начата, коммитов и push нет.

Прочитаны целиком: `docs/codex-brief.md`, затем документы раздела 1 по порядку: `AGENTS.md` → `README.md` → `docs/tracks/12-akim-tz.md` → `docs/akim-spec.md` → `docs/akim-backend-plan.md` (обе части) → `docs/akim-analysis-final-plan.md` → оба backend-review. Сверены фактические `app.py`, модули `engine/`, данные и тесты. Исторические шаги реализации из плана не выполнялись: текущая задача ограничена разделом 5 брифа.

## 1. Воспроизводимость

ОС: **Ubuntu 24.04.4 LTS**, Python **3.12.3**. Внутри этого клона создан `.venv`. Выполнено по README:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pytest -q
python -m uvicorn app:app --port 8000
```

Установка завершилась успешно. Это новая среда Python на существующей Ubuntu, не проверка другой ОС или полностью чистой машины. Песочница инструментов не запускает процессы (`bwrap: loopback ... Operation not permitted`), поэтому команды выполнялись через разрешённый запуск вне неё; это ограничение окружения агента, не проекта. `.env` не просматривался и не создавался; секреты не выводились. Внешние AI-запросы не выполнялись.

Вывод `pytest -q`:

```text
..........................................................               [100%]
=============================== warnings summary ===============================
.venv/lib/python3.12/site-packages/starlette/testclient.py:40
  /home/user/avm-ai-hackalem-review/.venv/lib/python3.12/site-packages/starlette/testclient.py:40: DeprecationWarning: The anyio.abc.BlockingPortal alias is deprecated, use anyio.from_thread.BlockingPortal instead.
    _PortalFactoryType = typing.Callable[[], typing.ContextManager[anyio.abc.BlockingPortal]]

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
58 passed, 1 warning in 0.87s
```

`GET /api/health` — **HTTP 200**, полный ответ:

```json
{"status":"ok","dataset_version":"hackalem-12-v1","ai_configured":false,"ranking_available":true,"total_plans":694395}
```

`POST /api/evaluate` — **HTTP 200** для тела:

```json
{"decisions":[{"measure_id":"M7","district_id":"nura"},{"measure_id":"M8","district_id":"nura"},{"measure_id":"M10","district_id":"nura"},{"measure_id":"M12"},{"measure_id":"M5","district_id":"saryarka"}]}
```

Ответ, сокращён только по полям (числа без округления):

```json
{
  "valid": true,
  "errors": [],
  "scenario_key": "M5:saryarka|M7:nura|M8:nura|M10:nura|M12",
  "cost": 95,
  "remaining_budget": 5,
  "score": 56.54307,
  "baseline_score": 52.55768,
  "delta": 3.9853900000000024,
  "decomposition": {
    "avg": 0.8506400000000021,
    "min": 1.1347499999999997,
    "crit": 2.0
  },
  "average": 58.077600000000004,
  "minimum": 52.9625,
  "critical_count": 0,
  "critical_pairs": [],
  "active_synergies": [
    "M10+M12"
  ],
  "rank": {
    "percentile": 99.9000999000999,
    "total": 694395,
    "top_position": null,
    "best_score": 57.236734999999996,
    "gap_to_best": 0.6936649999999958
  }
}
```

Также получены все 5 `district_results` по 10 показателей, 5 `measure_contributions`, синергия M10+M12: B1 +2 в Нуре. Полные HTTP-ответы локально сохранены в `.venv/codex-http-results.json` (игнорируется Git).

`POST /api/improvements`: лучший c1 заменяет M5 Сарыарка на M3 Нура, стоимость 100, Score 57.205560000000006, прирост 0.6624900000000054. `POST /api/explain`: HTTP 200, `mode: template`, `reason: not_configured`. Живой AI не проверен.

**Первый блокер приёмки:** `GET /` → **404**, `{"detail":"Not Found"}`; `static/index.html` отсутствует. `GET /docs` → HTTP 200 с HTML. Загрузка ресурсов Swagger браузером и работа офлайн не проверялись. API запускается, обещанный README сценарий через страницу пока не воспроизводится (известный блокер из брифа, подтверждён независимо).

## 2. До пяти предложений, по ценности / времени

На момент подготовки уже после 17:00: окно новых функций закрыто. Оценки ниже — оставшаяся работа после назначения исполнителя; никаких задач заочно не распределено. Изменения поведения требуют решения команды по заморозке, а проверки и документация могут идти до 17:40.

| № | Предложение и что увидит пользователь / эксперт | Файлы и зона | Время | Риск для сдачи | Зависимости | Срок |
|---|---|---|---|---|---|---|
| 1 | Честный путь приёмки: API работает; статус страницы явно указан, после поставки UI повторяется сценарий README. Защищает воспроизводимость (25 баллов) | `README.md` — Claude, только предложенный diff; `docs/codex-proposals.md` — Codex; сама страница — Алибек, `static/` | 5–10 мин на документацию и проверку, без оценки разработки UI | Низкий у документации; отсутствие UI остаётся высоким риском демо | Документация без ключа/UI; полная приёмка зависит от UI | Документация до 17:40; новых функций не добавлять |
| 2 | Некорректный ID и пустой ответ модели дают понятную ошибку вместо 500; ядро выдерживает нетипизированные входы | `engine/scoring.py`, `engine/explain.py`, `engine/validator.py` — Claude; будущие `tests/test_codex_regressions.py` — Codex | 10–15 мин с регрессиями | Низкий, локальные проверки входа | Без UI/реального ключа, подмена модели | Это исправления дефектов после 17:00, брать только по решению команды; отчёт до 17:40 |
| 3 | Убрать подтверждённые обходы сверки чисел; точнее описать пределы проверки и приближённость процентиля | `engine/explain.py`, `engine/templates.py`, `README.md` — Claude; тесты Codex после назначения | 15–20 мин | Средний: более строгая проверка может увеличить долю `template` | Без UI/ключа для тестов; качество live требует отдельной реальной проверки | Документация до 17:40; поведение — по решению о дефектах, новая функция до 17:00 уже не успевает |
| 4 | Зафиксировать независимые инварианты в воспроизводимых тестах: эксперт проверяет точность формулы, бюджет ровно 100, перестановки и ошибки ID | Новый `tests/test_codex_invariants.py` — зона Codex | 15 мин | Низкий; фиксированный seed и ограниченный объём | Не зависит от UI/ключа | Проверки до 17:40; сейчас выполнен эксперимент, файл тестов ещё не создан |
| 5 | Трёхминутное демо с готовыми наборами: ТЗ → c1 → глобальный лучший; отдельно отказ по бюджету и честная пометка `template` | Новый `docs/demo.md` — Codex, координация с Алибеком для показа UI | 10–15 мин | Низкий | Материал без ключа/UI; показ страницы зависит от Алибека | Материалы до 17:40, репетиция перед сдачей |

## 3. Подтверждённые находки и минимальные diff

Все diff ниже подготовлены к рассмотрению и не применены. Тестовые критерии описаны рядом; новые файлы тестов в рамках этого задания не создавались. README-патч фиксирует текущее состояние: если исправления/страница уже приняты командой, формулировки нужно обновить после повторной проверки.

### R1 — P1: README обещает недоступный интерфейс

`README.md:79`, раздел «Через интерфейс». В проверенном HEAD отсутствует `static/index.html`, а `app.py` монтирует статику только при его наличии. Шаг «Открыть localhost:8000» даёт 404. Исправление документации не заменяет поставку фронтенда. Минимальный diff также уточняет реально проверяемые гарантии чисел и ранга (R3/R6).

````diff
--- a/README.md
+++ b/README.md
@@ -76,7 +76,7 @@
 ```
 
 - Проверка, что сервер жив: http://localhost:8000/api/health отвечает `{"status":"ok","dataset_version":"hackalem-12-v1","ai_configured":false,"ranking_available":true,"total_plans":694395}` (`ai_configured: true`, если задан ключ OpenAI).
-- Страница приложения: http://localhost:8000 (сервер раздаёт `static/index.html`).
+- В проверенной версии `ee6aa94` страницы ещё нет: http://localhost:8000 отвечает 404. Основной сценарий проверяется через API ниже; `/docs` отдаёт Swagger UI (его ресурсы загружаются с CDN). После добавления `static/index.html` сервер нужно перезапустить и повторить проверку интерфейса.
 - Интерактивная документация API (Swagger, все маршруты можно вызвать из браузера): http://localhost:8000/docs.
 - Автотесты, 58 штук, без сети и без ключей: `pytest -q`.
 
@@ -156,7 +156,9 @@
    ```
    Без ключа: `"mode": "unavailable"`. С ключом: `"mode": "live"`, текст ответа и поле `numbers_checked`.
 
-### Через интерфейс
+### Через интерфейс (приёмочный сценарий после добавления фронтенда)
+
+В проверенной версии `ee6aa94` этот сценарий пока недоступен. Следующие шаги — план приёмки, а не подтверждённая функциональность. Упоминания вкладок, кнопок и панели в таблице соответствия ниже также относятся к плану интерфейса; сейчас подтверждён API.
 
 1. Открыть http://localhost:8000. Видна база: Score 52.56, два критических показателя в Нуре.
 2. Нажать «Пример из ТЗ»: набор M7 Нура, M8 Нура, M10 Нура, M12 город, M5 Сарыарка. Стоимость 95, Score 56.54, критических показателей нет.
@@ -190,16 +192,16 @@
 
 ### Режимы объяснения: live и template
 
-- `live`: сервер передаёт модели только посчитанные факты `f1…fN` и идентификаторы альтернатив `c1…c3`. Ответ проверяется: строгая JSON-схема, все `fact_ids` и `candidate_id` существуют, любое число в тексте совпадает с числом из фактов. Нарушение, таймаут или сетевая ошибка переводят ответ в `template`.
+- `live`: сервер передаёт модели только посчитанные факты `f1…fN` и идентификаторы альтернатив `c1…c3`. Ответ проверяется: строгая JSON-схема, все `fact_ids` и `candidate_id` существуют. Числа проверяются лексически с допуском 0,051, без проверки соответствия конкретному показателю или указанному факту. В текущей версии целые 0–14 пропускаются без сверки, знак Unicode-минус не распознаётся; это известные ограничения до исправления. Нарушение, таймаут или сетевая ошибка переводят ответ в `template`.
 - `template`: банк ответов без модели. Тексты собираются из тех же фактов, правил и полного перебора планов (`engine/templates.py`): резюме с рангом среди всех допустимых планов, сильные стороны, риски (в том числе упущенные синергии и непотраченный бюджет), предложения, сравнение с лучшим планом. Ответ помечен `mode: template` и причиной (`not_configured`, `JSONDecodeError`, `TimeoutError` и т.д.). Интерфейс показывает пометку «AI-советник недоступен, показано вычисленное объяснение».
-- Диалог `POST /api/chat` доступен только с ключом (серверным или из заголовка). Поле `numbers_checked` — лексическая проверка: каждое число ответа встречается среди чисел расчёта и топа планов. Смысл утверждений она не подтверждает; при `false` интерфейс показывает пометку «числа не сверены с расчётом».
+- Диалог `POST /api/chat` доступен только с ключом (серверным или из заголовка). Поле `numbers_checked` — результат той же лексической проверки с указанными выше ограничениями по малым числам и знаку; учитываются числа расчёта и топа планов. Смысл утверждений она не подтверждает; при `false` интерфейс показывает пометку «числа не сверены с расчётом».
 - Проверка чисел программная; качественные утверждения модели могут требовать оценки человеком. Живой режим и диалог покрыты тестами с подменённой моделью; реальный запрос к OpenAI командой не проверялся, так как на момент сдачи у команды не было ключа. Без ключа весь сценарий проходит в `template`.
 
 ### Ограничение советника
 
 `/api/improvements` перебирает замену одной меры на любую меру каталога с любым допустимым районом (до 270 вариантов), каждый кандидат проходит валидатор и расчёт. Возвращаются до трёх строгих улучшений, это «лучшее среди замен одной меры». Если улучшений нет, список пуст.
 
-Глобальную картину даёт индекс `data/top_sets.json`: он построен полным перебором всех 694 395 допустимых планов (`python scripts/rank_all.py`, около 20 секунд на CPU) и хранит топ-100 планов и 1000 квантилей Score. `GET /api/top` отдаёт лучшие планы, поле `rank` в `/api/evaluate` — процентиль плана (доля планов не лучше данного), позицию в топ-100 и разрыв до лучшего. Лучший план по этой модели: 57,23673 за 98 единиц; сам по себе он не является рекомендацией для реального города.
+Глобальную картину даёт индекс `data/top_sets.json`: он построен полным перебором всех 694 395 допустимых планов (`python scripts/rank_all.py`, около 20 секунд на CPU) и хранит топ-100 планов и 1001 выборочное значение Score (шаг 694 по отсортированным результатам). `GET /api/top` отдаёт лучшие планы, поле `rank` в `/api/evaluate` — приближённый процентиль по выборке (оценка доли планов не лучше данного), позицию в топ-100 и разрыв до лучшего. Лучший план по этой модели: 57,23673 за 98 единиц; сам по себе он не является рекомендацией для реального города.
 
 ## Сторонние материалы
 
````

Приёмка: команды раздела «Через API» проходят; после добавления страницы и перезапуска сервера `/` должен дать 200, затем ручная проверка всех пяти UI-шагов.

### R2 — P1: неизвестный Unicode-ID вызывает HTTP 500

`engine/scoring.py:19–20`. Заменить первое решение примера на `{"measure_id":"M²"}` или `{"measure_id":"M⁰"}`: `/api/evaluate` отвечает 500. Валидатор уже нашёл `unknown_measure`, но `evaluate` до возврата ошибок строит ключ; `str.isdigit()` допускает надстрочные цифры, которые `int()` не читает. Нарушен контракт HTTP 200 / `valid:false` для неизвестного строкового ID. Аналогично затронуты остальные маршруты, вызывающие `evaluate`.

````diff
--- a/engine/scoring.py
+++ b/engine/scoring.py
@@ -17,7 +17,8 @@
 
 
 def _measure_number(mid: str) -> int:
-    return int(mid[1:]) if mid[1:].isdigit() else 999
+    suffix = mid[1:]
+    return int(suffix) if len(suffix) <= 8 and suffix.isascii() and suffix.isdigit() else 999
 
 
 def scenario_key(decisions: list) -> str:
````

Приёмка: оба ID → evaluate 200, `valid:false`, `score:null`, `unknown_measure` с индексом 0; improvements/explain/chat → 422. Обычные M1–M14 сохраняют порядок и прежние ключи.

### R3 — P1: числовая сверка принимает выдуманное малое число и теряет минус

`engine/explain.py:17`, `_allowed_numbers`, `_numbers_ok`. На фактах контрольного примера `validate_answer` принимает корректную по схеме структуру с `summary: "Score 13"` и отдельно `summary: "Score −56,54"`. Причины: безусловный пропуск целых 0–14 и regex, не включающий Unicode-минус. С обычным числом `Score 56,54` принятие корректно. Проверено локально без модели и ключей.

````diff
--- a/engine/explain.py
+++ b/engine/explain.py
@@ -9,11 +9,11 @@
 from .facts import fallback_explanation
 from .templates import template_explanation
 
-PROMPT_VERSION = "akim-explain-v2"
+PROMPT_VERSION = "akim-explain-v3"
 MAX_CACHE = 100
 _cache: "OrderedDict[str, dict]" = OrderedDict()
 
-NUM_RE = re.compile(r"(?<![A-Za-zА-Яа-я\d_])-?\d+(?:[.,]\d+)?")
+NUM_RE = re.compile(r"(?<![A-Za-zА-Яа-я\d_])[+−-]?\d+(?:[.,]\d+)?")
 
 SYSTEM_PROMPT = (
     "Ты советник акима Астаны в учебном симуляторе городского бюджета. "
@@ -117,15 +117,13 @@
     for f in facts:
         walk(f.get("data", {}))
         for s in NUM_RE.findall(f["text"]):
-            vals.add(float(s.replace(",", ".")))
+            vals.add(float(s.replace(",", ".").replace("−", "-")))
     return vals
 
 
 def _numbers_ok(text: str, allowed: set) -> bool:
     for s in NUM_RE.findall(text):
-        v = float(s.replace(",", "."))
-        if v.is_integer() and 0 <= v <= 14:
-            continue
+        v = float(s.replace(",", ".").replace("−", "-"))
         if any(abs(v - a) <= 0.051 for a in allowed):
             continue
         return False
````

Это минимальная правка двух воспроизведённых обходов; версия промпта изменена для отделения старого кэша. Тесты: 13 при allowed={56.54307} и отрицательное −56,54 отвергаются; +56,54 и 56,5 принимаются; M7/S1/f2 не становятся самостоятельными числами. Контрольный корректный JSON остаётся `live`, некорректный → `template` с подменённой моделью.

Ограничение остаётся принципиальным: совпадение с любым числом всех фактов не доказывает правильный смысл или соответствие `fact_ids`. Например, стоимость может быть выдана за Score; числа словами и необычные форматы требуют отдельной политики. Патч не объявлять полной защитой от выдумок. README-diff R1 честно описывает текущие ограничения; после R3 удалить из него предложения про уже исправленные обходы, сохранив оговорку о смысле.

### R4 — P2: пустой ответ провайдера обрушает чат

`engine/explain.py`, конец `chat`. Подмена `chat_model=lambda *args: (None, "fake")` в `create_app`, валидный план и последнее сообщение `user`: HTTP 500. Сам вызов защищён `try`, но `_numbers_ok(None, ...)` выполняется за его пределами. Пустая строка сейчас тоже считается успешным ответом. Нужен существующий контракт `mode:error`, без нового формата ответа.

````diff
--- a/engine/explain.py
+++ b/engine/explain.py
@@ -236,6 +236,8 @@
     call = call_model or call_openai_chat
     try:
         text, model = call(system, trimmed, key)
+        if not isinstance(text, str) or not text.strip():
+            raise ValueError("empty model reply")
     except Exception as e:
         return {"mode": "error", "reply": f"Модель недоступна: {type(e).__name__}. Расчёт и шаблонное объяснение работают.", "numbers_checked": False, "model": None}
     extra = []
````

Приёмка с подменённой моделью: `None`, пустая/пробельная строка, нестроковый результат → HTTP 200, `mode:error`, `numbers_checked:false`; обычный текст сохраняет `live`.

### R5 — P3: прямой вызов ядра падает на ID-списке/объекте

`engine/validator.py:27,45`. `evaluate(city, [{"measure_id": []}] + example[1:])` и ID `{}` дают `TypeError` из-за хеширования. Через HTTP уже защищено Pydantic (422), поэтому это не блокер публичного API. Недочёт относится к заявленной устойчивости ядра и будущим прямым вызовам.

````diff
--- a/engine/validator.py
+++ b/engine/validator.py
@@ -24,7 +24,7 @@
             continue
         mid = d.get("measure_id")
         did = d.get("district_id") or None
-        m = city.measures.get(mid)
+        m = city.measures.get(mid) if isinstance(mid, str) else None
         if m is None:
             errors.append(_err("unknown_measure", f"Неизвестная мера {mid}", [mid] if mid else [], [i]))
             continue
@@ -42,7 +42,7 @@
 
     positions = defaultdict(list)
     for i, d in enumerate(decisions):
-        if isinstance(d, dict) and d.get("measure_id") in city.measures:
+        if isinstance(d, dict) and isinstance(d.get("measure_id"), str) and d["measure_id"] in city.measures:
             positions[d["measure_id"]].append(i)
     for mid, idx in positions.items():
         if len(idx) > 1:
````

Приёмка: прямые вызовы с `[]`/`{}` в measure_id → `valid:false`, `unknown_measure`; проверка повторов M1 без района остаётся рабочей. Патч не расширяет допустимые типы ID.

### R6 — P2: приближённый процентиль назван строгим превосходством

`engine/ranking.py:rank_info`, `engine/templates.py` (резюме), README. `bisect_right` считает выборочные значения **не выше** пользовательского Score; это оценка доли «не лучше», включающая равенства. Резюме пишет «Лучше N%», а README не оговаривает приближение. Для примера фактический percentile = 99.9000999000999 по 1001 выборочному значению. Топ-100 и разрыв до лучшего имеют другую, точную основу — их менять не требуется.

````diff
--- a/engine/templates.py
+++ b/engine/templates.py
@@ -109,9 +109,9 @@
         elif rank["top_position"]:
             parts.append(f"Это {rank['top_position']}-е место среди {rank['total']} вариантов.")
         elif rank["percentile"] >= 99.95:
-            parts.append("Лучше 99,9% всех вариантов.")
+            parts.append("По приближённому процентилю: не хуже примерно 99,9% вариантов.")
         else:
-            parts.append(f"Лучше {fmt(rank['percentile'])}% всех вариантов.")
+            parts.append(f"По приближённому процентилю: не хуже примерно {fmt(rank['percentile'])}% вариантов.")
     parts.append(f"Самый слабый район всё ещё {worst['name']}: {fmt(worst['score_after'])}.")
     summary = " ".join(parts)
 
````

Документационная часть — в R1. Приёмка: число и API-контракт ранга не меняются, пользователь видит слово «приближённому» и формулировку «не хуже». Точные места топ-100 остаются прежними. Новый алгоритм точного процентиля перед сдачей не предлагается.

## 4. Что проверено без найденных расхождений

- Все 58 штатных тестов проходят: контрольная база 52.55768, пример 56.54307, лаги, синергии, clip после суммы, строгое `<40`, перестановки, отсутствие мутаций, ошибки правил и проверка структуры HTTP.
- Дополнительный независимый расчёт через `Decimal` по сырому датасету: seed=230926, 2000 случайных наборов, 1153 допустимых. Для каждого совпали Score с допуском 1e-9, сумма разложения, результат полной перестановки; исходные данные не изменились. Расчёт-эталон не использовал `apply_effects`/`aggregate`.
- Отдельно проверен бюджет ровно 100 у c1; конфликт M5/M13 в одном районе отклонён, в разных разрешён.
- Старые находки о разложении кандидата от базы, `measure_id:null`, не-объектах и пропущенных повторах уже исправлены в соответствующих случаях; не представлены как новые.
- Полный индекс повторно построен в памяти: все 694 395 планов, топ-100 и 1001 выборочное значение полностью совпали с загруженным JSON (`fresh == load_index()`). Лучший Score 57.236734999999996. `data/top_sets.json` не перезаписывался.

Ограничения проверки: другая ОС и UI не проверены; живого запроса к OpenAI не было. При отсутствии ключа подтверждён `template`. При успешном `/docs` подтверждена отдача HTML, не офлайн-доступность ресурсов Swagger.

## 5. Состояние передачи

Сервер оставлен запущенным на http://127.0.0.1:8000; API доступен, `/` возвращает 404 до поставки фронтенда.

В рабочем дереве создан только этот документ. `.venv/` и сохранённые внутри него HTTP-ответы игнорируются Git. Зоны `static/`, `engine/`, `app.py`, `tests/`, `data/`, README не изменены. Коммиты, push, смена remote и ветки не выполнялись. Задачи ожидают распределения Канатом.

Проверка предложений: все шесть diff извлечены из этого документа; `git apply --check` прошёл, патчи не применялись. Штатные тесты запускались на исходном коде. Поведение предложенных исправлений ещё требует регрессионного запуска после согласования.
